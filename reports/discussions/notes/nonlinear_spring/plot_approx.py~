from numpy import *
import matplotlib.pyplot as plt

def H_W(x):
    return 1./(1.-x*x)

def H_C(x):
    return (1.-(1./3.)*x*x)/(1.-x*x)

H = 1.
x = linspace(0, 0.5, 20)
plt.plot(x, 
plt.plot(x, H_W(x), 'bo-', label='Warner approx.')
plt.plot(x, H_C(x), 'ro-', label
